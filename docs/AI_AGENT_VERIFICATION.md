# AI Agent Verification

Agent message proofs bind tenant, sender/recipient identities, message digest, timestamp, metadata, and an agent-message signing key. Raw prompts, transcripts, credentials, provider tokens, and private key material must not be stored as proof metadata.

