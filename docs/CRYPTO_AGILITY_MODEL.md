# Crypto Agility Model

Algorithms are selected through a versioned, operation- and context-bound registry. Unknown, disabled, type-hook-only, operation-mismatched, or context-invalid choices fail closed. ML-KEM, ML-DSA, and SLH-DSA remain non-runnable interface hooks with no protection claim.

