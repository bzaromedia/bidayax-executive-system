# Project-scoped Codex defaults for The Executive Card.
# Model defaults are intentionally not pinned here because the requested
# GPT-5.4 High Executor target is catalog-verified locally, but model-backed
# invocation could not be validated on this surface under current tenant policy.

[agents]
max_threads = 4
max_depth = 1
interrupt_message = true