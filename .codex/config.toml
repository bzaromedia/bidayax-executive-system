# Project-scoped Codex defaults for The Executive Card.
# Model defaults are intentionally not pinned here. Preferred models and the
# documented Executor fallback are maintained in docs/codex/MODEL-HIERARCHY.md.

[agents]
max_threads = 4
max_depth = 1
interrupt_message = true